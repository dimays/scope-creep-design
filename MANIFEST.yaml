name: scope-creep-design
description: Scope Creep's shared design system — runtime CSS-variable tokens + headless primitives, consumed by every app.
metadata:
  type: project
  status: active
  version: 0.3.0
  owner_agent: chief-designer
  last_verified: 2026-09-06
kind: package
stack: golden-path
spec: golden-path
consumed_via: git tag (semver); apps pin `github:dimays/scope-creep-design#vX.Y.Z`
